# Copy to .env and paste your key. Optional — the copilot degrades gracefully
# to grounded evidence (retrieved guidelines + sim numbers) without a key.
# Free key: https://console.groq.com/keys
GROQ_API_KEY=
