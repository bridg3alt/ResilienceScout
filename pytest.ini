[pytest]
# `resilienceos` lives under backend/, so put it on sys.path. Without this the suite only
# runs from inside backend/ and fails from the repo root, which is where people run it.
pythonpath = backend
testpaths = backend/tests
