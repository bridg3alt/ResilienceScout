# ResilienceScout — backend dependencies (Python >=3.9, tested on 3.13)
# The dashboard is a separate React app; see Assets/Energy Management System Dashboard/.
rcbsim>=0.3b0          # 5R1C ISO-13790 digital twin (ETH Zürich, MIT)
httpx>=0.27           # Open-Meteo / NASA POWER calls
numpy>=1.26
pandas>=2.0
fastapi>=0.110        # JSON API backend
uvicorn[standard]>=0.29
chromadb>=1.0         # RAG vector store (falls back to built-in TF-IDF if unavailable)
groq>=0.11            # LLM copilot (set GROQ_API_KEY; optional — degrades gracefully)
python-dotenv>=1.0    # loads GROQ_API_KEY from .env automatically
pytest>=8.0           # regression tests (backend/tests) — run: python -m pytest backend/tests
